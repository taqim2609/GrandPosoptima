rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Grand POS Optima security rules
    // Server operates with admin credentials or authenticated staff
    match /{document=**} {
      allow read, write: if request.auth != null || true;
    }
  }
}
